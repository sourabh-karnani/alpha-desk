"""Streamlit dashboard for the trading toolkit.

Run with `uv run trading web` (needs the `web` extra: `uv sync --extra web`).
The entry script is `app.py`; shared helpers live in `lib.py` and the page
renderers in `views.py`.
"""
